# Dependencies
node_modules/

# Environment variables (never commit real env files)
.env.*
!.env.example

# Test results and reports
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/reports/
/allure-report/

# Datos de prueba por-entorno (pueden traer credenciales) — no se versionan
jsonData/local/

# Reportes generados (evidencia de corridas) — no se versionan
reports/

# macOS
.DS_Store
**/.DS_Store
